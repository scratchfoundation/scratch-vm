export = ArgumentType;
/**
 * Block argument types
 */
type ArgumentType = string;
declare namespace ArgumentType {
    let ANGLE: string;
    let BOOLEAN: string;
    let COLOR: string;
    let NUMBER: string;
    let STRING: string;
    let MATRIX: string;
    let NOTE: string;
    let IMAGE: string;
}
