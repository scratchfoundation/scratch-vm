const test = require('tap').test;
const Base64Util = require('../../src/util/base64-util');

test('base64ToUint8Array', t => {
    t.strictEqual(Base64Util.base64ToUint8Array(/* TODO */), /* TODO */);
    t.end();
});

test('uint8ArrayToBase64', t => {
    t.strictEqual(Base64Util.uint8ArrayToBase64(/* TODO */), /* TODO */);
    t.end();
});
